---
advisories:
- affected_versions:
  - <2.026
  cves: []
  description: |
    The function croak is variadic which expects as a first parameter printf-style format.  Passing arbitrary and string from the caller as a printf format leads to the security problem CWE-134: Use of Externally-Controlled Format String.
  fixed_versions:
  - '>=2.027'
  github_security_advisory: []
  id: CPANSA-Socket-2017-01
  references:
  - https://rt.cpan.org/Public/Bug/Display.html?id=122830
  reported: 2017-08-17
  severity: ~
- affected_versions:
  - <2.041
  cves:
  - CVE-2026-12087
  description: Socket versions before 2.041 for Perl have an out-of-bounds heap read.  In
    Socket.xs, pack_ip_mreq_source() checks the length of its source argument before
    the argument is read, so the check tests the byte length carried over from the
    preceding multiaddr argument instead. Both addresses occupy a 4-byte field, so
    a valid multiaddr lets a source of any length pass the check, and the source is
    then copied into the 4-byte imr_sourceaddr field with a fixed-size copy. A source
    shorter than 4 bytes is not rejected, and the copy reads up to 3 bytes past the
    end of its buffer.  Calling pack_ip_mreq_source() with a source value shorter
    than 4 bytes copies adjacent heap memory into the returned packed structure.
  fixed_versions:
  - '>=2.041'
  github_security_advisory:
  - GHSA-q5cv-5vf6-95gp
  id: CPANSA-Socket-2026-12087
  references:
  - https://github.com/Perl/perl5/commit/de19a0b0ad1900fef976c5c1400bd8f11ec6c6cb.patch
  - https://metacpan.org/release/PEVANS/Socket-2.041/changes
  - http://www.openwall.com/lists/oss-security/2026/06/15/10
  reported: 2026-06-15
  severity: ~
cpansa_version: 2
distribution: Socket
last_checked: 1773231586
latest_version: '2.040'
metacpan: https://metacpan.org/pod/Socket
repo: https://github.com/Perl/perl5
