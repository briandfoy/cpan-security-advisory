---
advisories:
- affected_versions:
  - '>=0.10,<=0.13'
  cves:
  - CVE-2025-40911
  description: Net::CIDR::Set versions 0.10 through 0.13 for Perl does not properly
    handle leading zero characters in IP CIDR address strings, which could allow attackers
    to bypass access control that is based on IP addresses.  Leading zeros are used
    to indicate octal numbers, which can confuse users who are intentionally using
    octal notation, as well as users who believe they are using decimal notation.  Net::CIDR::Set
    used code from Net::CIDR::Lite, which had a similar vulnerability CVE-2021-47154.
  fixed_versions:
  - '>=0.14'
  github_security_advisory:
  - GHSA-p745-hrr6-rg8c
  id: CPANSA-Net-CIDR-Set-2025-40911
  references:
  - https://blog.urth.org/2021/03/29/security-issues-in-perl-ip-address-distros/
  - https://github.com/robrwo/perl-Net-CIDR-Set/commit/be7d91e8446ad8013b08b4be313d666dab003a8a.patch
  - https://metacpan.org/release/RRWO/Net-CIDR-Set-0.14/changes
  reported: 2025-05-27
  severity: ~
- affected_versions:
  - <0.21
  cves:
  - CVE-2026-49940
  description: Net::CIDR::Set versions through 0.20 for Perl accept non-ASCII IP addresses
    and netmasks.  Unicode digits such as the Arabic-Indic One (U+0661) were accepted
    but not properly parsed as numbers.  This could allow network masks to accept
    larger networks.
  fixed_versions:
  - '>=0.21'
  github_security_advisory:
  - GHSA-hpv8-77xm-27wx
  id: CPANSA-Net-CIDR-Set-2026-49940
  references:
  - https://metacpan.org/release/RRWO/Net-CIDR-Set-0.21/changes
  - https://nvd.nist.gov/vuln/detail/CVE-2025-40911
  reported: 2026-06-04
  severity: ~
- affected_versions:
  - <0.21
  cves:
  - CVE-2026-49941
  description: Net::CIDR::Set versions through 0.20 for Perl did not validate IP addresses.  The
    add method called the _encode method to parse addresses. If the addresses did
    not look like netmasks or network ranges, then they were assumed to single IP
    addresses and passed back to itself as a 32-bit or 128-bit netmask.  If the argument
    was not a well-formed IP address, then this would lead to indefinite recursion.  An
    attacker could use this to cause a denial of service.
  fixed_versions:
  - '>=0.21'
  github_security_advisory:
  - GHSA-wvxw-q92g-9ghf
  id: CPANSA-Net-CIDR-Set-2026-49941
  references:
  - https://metacpan.org/release/RRWO/Net-CIDR-Set-0.21/changes
  - http://www.openwall.com/lists/oss-security/2026/06/04/11
  reported: 2026-06-04
  severity: ~
- affected_versions:
  - <0.21
  cves:
  - CVE-2026-49942
  description: Net::CIDR::Set versions through 0.20 for Perl did not validate network
    masks.  The mask portion of a network mask could contain Unicode digits such as
    the Arabic-Indic One (U+0661), or non-digits, which were ignored.   This could
    allow network masks to accept larger networks.  Leading zeros were also accepted,
    but treated as decimal instead of octal.  This could lead to confusion about what
    networks are acceptable.
  fixed_versions:
  - '>=0.21'
  github_security_advisory:
  - GHSA-fff7-gx98-vr3g
  id: CPANSA-Net-CIDR-Set-2026-49942
  references:
  - https://metacpan.org/release/RRWO/Net-CIDR-Set-0.21/changes
  - https://nvd.nist.gov/vuln/detail/CVE-2025-40911
  - https://nvd.nist.gov/vuln/detail/CVE-2026-45191
  reported: 2026-06-04
  severity: ~
- affected_versions:
  - <0.23
  cves:
  - CVE-2026-19566
  description: |-
    Net::CIDR::Set versions before 0.23 for Perl allow memory exhaustion and malformed set ranges via unbounded IPv6 prefix lengths.

    The _encode method accepts any prefix length matching `(0|[1-9][0-9]*)` and passes it to _width2bits(), which builds the mask as `'1' x ($width + 8)`, one character per bit. The _inc() method then unpacks the packed mask into a Perl array of one scalar per byte, so the prefix length alone sets the allocation size: `::/100000000` builds a 100 MB string and a 12.5 million element array. The value being tested is parsed, not just the configured ranges: contains() builds a set from its argument, and _guess_coder() tries the IPv4 coder and then the IPv6 coder, so an IPv4-only set expands an oversized IPv6 prefix length before the mixed address width check rejects it.

    Any caller that passes untrusted input to contains() or add() can exhaust process memory. A prefix length above 128 is also stored as a range that does not match the requested block: 2001:db8::/129 stringifies back unchanged, contains() of its own base address returns false, and removing it from a set drops the base address while the set still prints as covering it.
  fixed_versions:
  - '>=0.23'
  github_security_advisory:
  - GHSA-grjr-r4x5-mx4p
  id: CPANSA-Net-CIDR-Set-2026-19566
  references:
  - https://github.com/robrwo/perl-Net-CIDR-Set/commit/e16b27db676fd1ca671fbb31208a22c1b1ba9724.patch
  - https://github.com/robrwo/perl-Net-CIDR-Set/security/advisories/GHSA-grjr-r4x5-mx4p
  - https://metacpan.org/release/RRWO/Net-CIDR-Set-0.23/changes
  - https://www.cve.org/CVERecord?id=CVE-2026-49942
  reported: 2026-08-12
  severity: high
cpansa_version: 2
darkpan: ~
distribution: Net-CIDR-Set
last_checked: 1785398915
latest_version: '0.22'
metacpan: https://metacpan.org/pod/Net::CIDR::Set
